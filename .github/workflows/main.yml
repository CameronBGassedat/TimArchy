name: Main workflow

on:
  push:

jobs:
  lint:
    name: Lint for ts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd ./PulserNode && npm install
      - run: cd ./PulserNode && npm run lint